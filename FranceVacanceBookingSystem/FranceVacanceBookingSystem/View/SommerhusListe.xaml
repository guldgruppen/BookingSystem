<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FranceVacanceBookingSystem.View"
    xmlns:common="using:FranceVacanceBookingSystem.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:FranceVacanceBookingSystem.ViewModel"
    x:Name="pageRoot"
    x:Class="FranceVacanceBookingSystem.View.SommerhusListe"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d">

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">My Application</x:String>

    </Page.Resources>

    <!--
        This grid acts as a root panel for the page that defines two rows:
        * Row 0 contains the back button and page title
        * Row 1 contains the rest of the page layout
    -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="22"/>
        </Grid.ColumnDefinitions>
        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="ms-appx:///Assets/louvre-museum.jpg"/>
        </Grid.Background>
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition/>
            <RowDefinition Height="26"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid.DataContext>
            <ViewModel:SommerhusKatalog/>
        </Grid.DataContext>

        <!-- Back button and page title -->
        <Grid Grid.ColumnSpan="3" Background="#CC192B46" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin">
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
				Style="{StaticResource NavigationBackButtonNormalStyle}"
				VerticalAlignment="Top"
				AutomationProperties.Name="Back"
				AutomationProperties.AutomationId="BackButton"
				AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" 
				IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="106,0,32,40"/>
        </Grid>
        <ListView x:Name="listView" Grid.Column="1" Margin="20,5,0,0" Grid.Row="1" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin, LeftPosition, RightPosition, TopPosition, BottomPosition" Background="#192B46" ItemsSource="{Binding Sommerhuse, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" >
            <ListView.Resources>
                <Style x:Key="ItemStyleSommerhusListe" TargetType="ListViewItem">
                    <Setter Property="FontFamily" Value="{ThemeResource ContentControlThemeFontFamily}"/>
                    <Setter Property="FontSize" Value="{ThemeResource ControlContentThemeFontSize}"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="TabNavigation" Value="Local"/>
                    <Setter Property="IsHoldingEnabled" Value="True"/>
                    <Setter Property="Margin" Value="0,0,18,2"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListViewItem">
                                <ListViewItemPresenter CheckHintBrush="{ThemeResource ListViewItemCheckHintThemeBrush}" CheckBrush="{ThemeResource ListViewItemCheckThemeBrush}" ContentMargin="4" ContentTransitions="{TemplateBinding ContentTransitions}" CheckSelectingBrush="{ThemeResource ListViewItemCheckSelectingThemeBrush}" DragForeground="{ThemeResource ListViewItemDragForegroundThemeBrush}" DragOpacity="{ThemeResource ListViewItemDragThemeOpacity}" DragBackground="{ThemeResource ListViewItemDragBackgroundThemeBrush}" DisabledOpacity="{ThemeResource ListViewItemDisabledThemeOpacity}" FocusBorderBrush="{ThemeResource ListViewItemFocusBorderThemeBrush}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" Padding="{TemplateBinding Padding}" PointerOverBackgroundMargin="1" PlaceholderBackground="{ThemeResource ListViewItemPlaceholderBackgroundThemeBrush}" PointerOverBackground="{ThemeResource ListViewItemPointerOverBackgroundThemeBrush}" ReorderHintOffset="{ThemeResource ListViewItemReorderHintThemeOffset}" SelectedPointerOverBorderBrush="#FF74BAE8" SelectionCheckMarkVisualEnabled="True" SelectedForeground="{ThemeResource ListViewItemSelectedForegroundThemeBrush}" SelectedPointerOverBackground="#FF74BAE8" SelectedBorderThickness="{ThemeResource ListViewItemCompactSelectedBorderThemeThickness}" SelectedBackground="#FF74BAE8" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <StaticResource ResourceKey="ItemStyleSommerhusListe"/>

            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid HorizontalAlignment="Stretch" Margin="0" Background="#CC415471" Width="auto" Height="175">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="178"/>
                            <ColumnDefinition Width="22*"/>
                            <ColumnDefinition Width="132"/>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="32*"/>
                            <ColumnDefinition Width="202"/>
                            <ColumnDefinition Width="59*"/>
                            <ColumnDefinition Width="220"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Height="173" VerticalAlignment="Top" Margin="0,0,1,0" d:LayoutOverrides="LeftPosition, RightPosition">
                            <TextBlock x:Name="textBlock" TextWrapping="Wrap" Text="Val Thorens" HorizontalAlignment="Center" FontSize="29.333"/>
                            <Image x:Name="image" Height="133" Source="ms-appx:///Assets/Sommerhus i Frankrig2.jpg" Margin="0"/>
                        </StackPanel>
                        <StackPanel Margin="4,0,0,0" d:LayoutOverrides="Width, LeftPosition, RightPosition" Height="173" VerticalAlignment="Top" Grid.Column="2">
                            <TextBlock x:Name="textBlock1" TextWrapping="Wrap" Text="Antal Badeværelser:" Margin="0,40,0,0"/>
                            <TextBlock x:Name="textBlock2" TextWrapping="Wrap" Text="Antal Soveværelser:" Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock3" TextWrapping="Wrap" Text="Størrelse:" Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock4" TextWrapping="Wrap" Text="Husdyr tilladt:" Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock5" TextWrapping="Wrap" Text="Swimmingpool:" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="13,0,0,0" Grid.Column="3" Height="173" VerticalAlignment="Top" d:LayoutOverrides="LeftPosition, RightPosition">
                            <TextBlock x:Name="textBlock6" TextWrapping="Wrap" Text="4" Margin="0,40,0,0"/>
                            <TextBlock x:Name="textBlock7" TextWrapping="Wrap" Text="4" Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock8" TextWrapping="Wrap" Text="160 " Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock9" TextWrapping="Wrap" Text="ja" Margin="0,5,0,0"/>
                            <TextBlock x:Name="textBlock10" TextWrapping="Wrap" Text="nej" Margin="0,5,0,0"/>
                        </StackPanel>
                        <StackPanel Margin="9,0,0,0" d:LayoutOverrides="LeftPosition, RightPosition" Grid.Column="5" Height="173" VerticalAlignment="Top">
                            <TextBlock x:Name="textBlock11" TextWrapping="Wrap" Text="3.400 DKK" Margin="0,100,0,0" FontSize="40"/>
                        </StackPanel>
                        <StackPanel Margin="16,16,0,0" Grid.Column="7" d:LayoutOverrides="Width, LeftPosition, RightPosition">
                            <Button x:Name="button" Content="Tilføj til favoritter" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FFF9A716" Margin="0" FontSize="21.333"/>
                            <Button x:Name="button1" Content="Mere Info" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FFCB2929" FontSize="21.333"/>
                            <Button x:Name="button2" Content="Book" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#FF1AAA13" FontSize="21.333"/>
                        </StackPanel>
                    </Grid>

                </DataTemplate>
            </ListView.ItemTemplate>

        </ListView>
        <StackPanel Margin="0,5,0,0" Grid.Row="1" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin, TopPosition, BottomPosition" Background="#FFE81A1A"/>

    </Grid>
</Page>
